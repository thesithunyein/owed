{
  "name": "owed-keeper",
  "version": "0.1.0",
  "description": "Owed keeper: reader-trap classification, scaled reader, snapshot builder, Merkle registers",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "gen:vectors": "node ../scripts/gen-vectors.mjs"
  }
}
