{
  "name": "owed-keeper",
  "version": "0.1.0",
  "description": "Owed keeper: snapshot builder, Merkle registers, divergence monitor",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "gen:vectors": "node scripts/gen-vectors.mjs"
  }
}
