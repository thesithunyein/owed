/target
**/target
node_modules/
dist/
# Deploy output, regenerated by scripts/build-site.mjs
/site
# Written by tests/owed.mjs on every run. The committed record of a specific run
# belongs in docs/devnet-settlement-<date>.json instead, so this stays untracked
# rather than churning on every local or CI execution.
tests/settlement-report.json
.anchor/
test-ledger/
.env
*.log
.DS_Store
